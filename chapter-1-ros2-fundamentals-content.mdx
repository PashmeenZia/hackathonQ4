---
title: "ROS 2 Fundamentals"
sidebar_label: "Chapter 1: ROS 2 Fundamentals"
description: "Learn the fundamentals of ROS 2 architecture, Nodes, Topics, Services, and Parameters"
---

# Chapter 1: ROS 2 Fundamentals

## Overview of ROS 2 Architecture

ROS 2 (Robot Operating System 2) is a flexible framework for writing robot software. It's a collection of tools, libraries, and conventions that aim to simplify the task of creating complex and robust robot behavior across a wide variety of robot platforms.

### Key Architecture Concepts

ROS 2 uses a distributed architecture where computation is spread across many different processes, potentially on multiple machines connected by a network. The key components are:

- **Nodes**: Basic computational elements that perform processing
- **Topics**: Named buses over which nodes exchange messages
- **Services**: Synchronous request/response communication
- **Parameters**: Configuration values accessible to nodes
- **Actions**: Asynchronous, goal-oriented communication

### DDS (Data Distribution Service)

ROS 2 uses DDS as its underlying communication middleware. DDS provides the publish/subscribe communication pattern that enables the decoupled architecture of ROS 2.

## Nodes

Nodes are the fundamental building blocks of ROS 2 applications. Each node performs a specific task and communicates with other nodes through topics, services, or parameters.

### Creating a Node in Python

Here's a basic example of a ROS 2 node using the rclpy library:

```python
import rclpy
from rclpy.node import Node

class MinimalPublisher(Node):

    def __init__(self):
        super().__init__('minimal_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5  # seconds
        self.timer = self.create_timer(timer_period, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = f'Hello World: {self.i}'
        self.publisher_.publish(msg)
        self.get_logger().info(f'Publishing: "{msg.data}"')
        self.i += 1

def main(args=None):
    rclpy.init(args=args)
    minimal_publisher = MinimalPublisher()
    rclpy.spin(minimal_publisher)
    minimal_publisher.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### Node Lifecycle

Nodes in ROS 2 can have a lifecycle that includes:
- Unconfigured
- Inactive
- Active
- Finalized

This allows for more sophisticated management of robot components.

## Topics and Messaging

Topics are the primary method of communication in ROS 2. They use a publish/subscribe pattern where nodes can publish messages to a topic and other nodes can subscribe to that topic to receive messages.

### Publisher Example

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class PublisherExample(Node):

    def __init__(self):
        super().__init__('publisher_example')
        self.publisher_ = self.create_publisher(String, 'chatter', 10)
        timer_period = 0.5  # seconds
        self.timer = self.create_timer(timer_period, self.timer_callback)

    def timer_callback(self):
        msg = String()
        msg.data = 'Hello from publisher!'
        self.publisher_.publish(msg)
        self.get_logger().info(f'Publishing: "{msg.data}"')

def main(args=None):
    rclpy.init(args=args)
    publisher_example = PublisherExample()
    rclpy.spin(publisher_example)
    publisher_example.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### Subscriber Example

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class SubscriberExample(Node):

    def __init__(self):
        super().__init__('subscriber_example')
        self.subscription = self.create_subscription(
            String,
            'chatter',
            self.listener_callback,
            10)
        self.subscription  # prevent unused variable warning

    def listener_callback(self, msg):
        self.get_logger().info(f'I heard: "{msg.data}"')

def main(args=None):
    rclpy.init(args=args)
    subscriber_example = SubscriberExample()
    rclpy.spin(subscriber_example)
    subscriber_example.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

## Services

Services provide synchronous request/response communication between nodes. A service client sends a request to a service server, which processes the request and returns a response.

### Service Server Example

```python
from add_two_ints_srv.srv import AddTwoInts
import rclpy
from rclpy.node import Node

class MinimalService(Node):

    def __init__(self):
        super().__init__('minimal_service')
        self.srv = self.create_service(AddTwoInts, 'add_two_ints', self.add_two_ints_callback)

    def add_two_ints_callback(self, request, response):
        response.sum = request.a + request.b
        self.get_logger().info(f'Incoming request\na: {request.a}, b: {request.b}')
        return response

def main(args=None):
    rclpy.init(args=args)
    minimal_service = MinimalService()
    rclpy.spin(minimal_service)
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### Service Client Example

```python
import sys
from add_two_ints_srv.srv import AddTwoInts
import rclpy
from rclpy.node import Node

class MinimalClientAsync(Node):

    def __init__(self):
        super().__init__('minimal_client_async')
        self.cli = self.create_client(AddTwoInts, 'add_two_ints')
        while not self.cli.wait_for_service(timeout_sec=1.0):
            self.get_logger().info('service not available, waiting again...')
        self.req = AddTwoInts.Request()

    def send_request(self, a, b):
        self.req.a = a
        self.req.b = b
        self.future = self.cli.call_async(self.req)
        rclpy.spin_until_future_complete(self, self.future)
        return self.future.result()

def main():
    rclpy.init()

    minimal_client = MinimalClientAsync()
    response = minimal_client.send_request(int(sys.argv[1]), int(sys.argv[2]))
    minimal_client.get_logger().info(
        f'Result of add_two_ints: {response.sum}')

    minimal_client.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

## Parameters

Parameters in ROS 2 are named values that can be configured at runtime. They provide a way to configure nodes without recompiling.

### Parameter Example

```python
import rclpy
from rclpy.node import Node

class ParametersExample(Node):

    def __init__(self):
        super().__init__('parameters_example')

        # Declare parameters with default values
        self.declare_parameter('my_parameter', 'default_value')
        self.declare_parameter('integer_param', 42)
        self.declare_parameter('double_param', 3.14)

        # Get parameter values
        my_param = self.get_parameter('my_parameter').value
        integer_param = self.get_parameter('integer_param').value
        double_param = self.get_parameter('double_param').value

        self.get_logger().info(f'my_parameter: {my_param}')
        self.get_logger().info(f'integer_param: {integer_param}')
        self.get_logger().info(f'double_param: {double_param}')

def main(args=None):
    rclpy.init(args=args)
    node = ParametersExample()
    rclpy.spin(node)
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

## Practical Exercises

### Exercise 1: Publisher/Subscriber Pair
Create a publisher that publishes temperature readings and a subscriber that logs these readings.

### Exercise 2: Service Implementation
Implement a service that converts temperature between Celsius and Fahrenheit.

### Exercise 3: Parameter Configuration
Create a node that uses parameters to control its behavior (e.g., frequency of publishing).

## Summary

This chapter covered the fundamental concepts of ROS 2:
- Nodes as the basic computational elements
- Topics for publish/subscribe communication
- Services for request/response communication
- Parameters for configuration

Understanding these concepts is essential for working with ROS 2 and forms the foundation for more advanced topics.